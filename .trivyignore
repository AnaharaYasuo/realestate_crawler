# ------------------------------------------------------------------------------
# Trivy Misconfiguration Ignore List
# ------------------------------------------------------------------------------
# 過去に作成された停止中(TERMINATED)のレガシーバックアップVM (backup2022-02-01) の既知リスク設定。
# 将来再稼働時に専用サービスアカウントへの切り替えおよび外部IP削除を実施する前提で、
# 現在はTerraformによる現状資産管理(Import)を目的としているためスキャン対象から除外。
AVD-GCP-0031
GCP-0031
AVD-GCP-0044
GCP-0044
