name: Snyk Security Scan

on:
  push:
    branches:
      - master
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.filter.outputs.code }}
      iac: ${{ steps.filter.outputs.iac }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2
      - name: Path Filter
        uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            code:
              - 'src/**'
              - 'config/**'
              - 'Dockerfile'
              - 'docker-compose.yml'
              - '.github/workflows/snyk.yml'
            iac:
              - 'terraform/**'
              - '.trivyignore'
              - '.github/workflows/snyk.yml'

  # NOTE: Snyk is temporarily disabled in favor of Trivy, Semgrep, and Checkov
  # defined in .github/workflows/security-scan.yml
  # snyk:
  #   name: Snyk Analysis
  #   needs: changes
  #   if: ${{ needs.changes.outputs.code == 'true' || needs.changes.outputs.iac == 'true' }}
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4.2.2
  #
  #     - name: Set up Python
  #       uses: actions/setup-python@82c7e631bb3cdc910f68e0081d67478d79c6982d # v5.1.0
  #       with:
  #         python-version: '3.11'
  #
  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r src/crawler/requirements.txt
  #
  #     - name: Install Snyk CLI
  #       uses: snyk/actions/setup@9adf32b1121593767fc3c057af55b55db032dc04 # v1.0.0
  #
  #     - name: Run Snyk Open Source (Python dependencies)
  #       if: env.SNYK_TOKEN != ''
  #       run: snyk test --file=src/crawler/requirements.txt --skip-unresolved
  #       env:
  #         SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
  #
  #     - name: Run Snyk Code (SAST)
  #       if: env.SNYK_TOKEN != ''
  #       run: snyk code test
  #       continue-on-error: true
  #       env:
  #         SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
  #
  #     - name: Build Docker image for analysis
  #       if: env.SNYK_TOKEN != ''
  #       run: docker build -t realestate-crawler:test .
  #
  #     - name: Run Snyk Container (Docker image vulnerabilities)
  #       if: env.SNYK_TOKEN != ''
  #       run: snyk container test realestate-crawler:test --file=Dockerfile --severity-threshold=high --exclude-base-image-vulns
  #       continue-on-error: true
  #       env:
  #         SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
  #
  #     - name: Run Snyk IaC (Terraform Infrastructure Misconfigurations)
  #       if: env.SNYK_TOKEN != ''
  #       run: snyk iac test terraform/ --severity-threshold=high
  #       continue-on-error: true
  #       env:
  #         SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  trivy-iac:
    name: Trivy IaC Security Scan
    needs: changes
    if: ${{ needs.changes.outputs.iac == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Run Trivy vulnerability scanner in IaC mode
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'config'
          scan-ref: 'terraform/'
          severity: 'CRITICAL,HIGH'
          trivyignores: '.trivyignore'
          exit-code: '1'
