name: Deploy Production

on:
  push:
    branches:
      - production

jobs:
  test-and-scan:
    name: Run Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Run Trivy vulnerability scanner in IaC mode
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'config'
          scan-ref: 'terraform/'
          severity: 'CRITICAL,HIGH'
          exit-code: '1'

  terraform-apply:
    name: Terraform Apply (Production Infrastructure)
    needs: test-and-scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.0

      - name: Terraform Init
        working-directory: ./terraform
        run: terraform init

      - name: Terraform Apply
        working-directory: ./terraform
        run: terraform apply -auto-approve -var="project_id=sumifu" -var="environment=prod"

  build-and-deploy-container:
    name: Build & Deploy Container to Cloud Run
    needs: terraform-apply
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker asia-northeast1-docker.pkg.dev --quiet

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            asia-northeast1-docker.pkg.dev/sumifu/realestate-crawler/crawler:latest
            asia-northeast1-docker.pkg.dev/sumifu/realestate-crawler/crawler:${{ github.sha }}

      - name: Update Cloud Run Job Image
        run: |
          gcloud run jobs update realestate-crawler-pipeline-prod \
            --image=asia-northeast1-docker.pkg.dev/sumifu/realestate-crawler/crawler:${{ github.sha }} \
            --region=asia-northeast1

      - name: Update Cloud Run Service Image
        run: |
          gcloud run services update realestate-slack-agent-prod \
            --image=asia-northeast1-docker.pkg.dev/sumifu/realestate-crawler/crawler:${{ github.sha }} \
            --region=asia-northeast1
